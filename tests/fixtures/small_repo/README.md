# Small Evidence Repo

This project demonstrates a bounded command-line tool with tests and explicit
limitations.

The system is not production-ready. It is intended as a reviewer-facing example
with a minimal reproducible path.
